# MCP Transport Configuration
# Transport mode: 'stdio' (default) or 'http'
MCP_TRANSPORT=stdio

# HTTP Server Configuration (only used when MCP_TRANSPORT=http)
# Host to bind the HTTP server to
# Use '0.0.0.0' to accept connections from any IP (for VM use)
# Use '127.0.0.1' to only accept local connections
MCP_HTTP_HOST=0.0.0.0

# Port for the HTTP server
MCP_HTTP_PORT=3000

# Screenshot Caching Configuration
# Set to 'true' to save screenshots to disk for debugging
SCREENSHOT_CACHE=false
